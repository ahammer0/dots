# ajouter symfony cli
+composer
+ajouter l'install de ly + config

# ajouter themes alacritty
-ajouter le fichier de config alacritty 

# ajouter pnpm
- npm install -g pnpm

#vim coc
-ajouter coc-css
